pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'docker -v'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'whoami'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'docker ps'
            }
        }
    }
}
